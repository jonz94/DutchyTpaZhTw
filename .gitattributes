# auto detect text files and perform LF normalization
* text=auto

# make sure Windows batch files will always have CRLF line endings
*.bat text eol=crlf

# denote .jar files that are truly binary and should not be modified by git
*.jar binary
